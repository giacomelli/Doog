 <Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">
      <Target Name="AfterBuild">
          <ItemGroup>
              <FilesToCopy Include="$(MSBuildProjectDirectory)\bin\$(Configuration)\**\*.*" />
          </ItemGroup>

          <Message Text="### Copying $(MSBuildProjectName) files ..." Importance="high" />

          <Copy
              SourceFiles="@(FilesToCopy)"
              DestinationFiles="@(FilesToCopy->'$(MSBuildProjectDirectory)\..\Doog.Runners.Console\bin\Debug\%(RecursiveDir)%(Filename)%(Extension)')"/>


          <Copy
              SourceFiles="@(FilesToCopy)"
              DestinationFiles="@(FilesToCopy->'$(MSBuildProjectDirectory)\..\Doog.Runners.Unity\Assets\Scripts\references\%(RecursiveDir)%(Filename)%(Extension)')"/>
    </Target>
</Project>
